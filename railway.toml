[build]
builder = "nixpacks"

[build.nixpacks]
nixPkgs = ["nodejs_18"]

[build.env]
NPM_CONFIG_PRODUCTION = "false"
NPM_CONFIG_OMIT = ""

[deploy]
startCommand = "cd backend && npx ts-node prisma/seed-super-admin.ts; cd .. && npm run start"
